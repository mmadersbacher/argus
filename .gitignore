# Rust
/target
**/*.rs.bk

# Env / secrets
.env
.env.*
!.env.example

# Web
node_modules
/web/node_modules
/web/.next
/web/out
/web/dist

# Eval real-data snapshots (large, drift daily, fetched via scripts/)
/crates/argus-eval/data/real/

# Misc
*.log
.DS_Store

# Runtime IntelCache snapshot (written to cwd by argus-api)
intel-cache.json
deploy/school/.env.school
# School appliance: real env stays ignored (.env.*); template is tracked.
!deploy/school/.env.school.example
